# --- Production (Koyeb) — FTPS only, no RDBMS ---

# Deactivate JDBC/JPA autoconfiguration (we do not use a DB in prod)
spring.autoconfigure.exclude=\
  org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration,\
  org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration

# Feature flags for prod
# (FTPS persistence lives outside JPA; keep this as you prefer)
features.persistentRooms.enabled=true

# Allowed origins
app.websocket.allowed-origins=http://localhost:*,http://127.0.0.1:*,https://*.trycloudflare.com,https://ep.noxvobiscum.at

# Honor proxy/CDN headers
server.forward-headers-strategy=framework
