# App-Name
spring.application.name=estpoker

# Datenbankverbindung – Umgebungsvariablen bevorzugt, sonst Defaults (lokal)
spring.datasource.url=${DB_URL:jdbc:mysql://mysql:3306/db171085_6?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USER:db171085_6}
spring.datasource.password=${DB_PASS:lokales-passwort}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Einstellungen
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
