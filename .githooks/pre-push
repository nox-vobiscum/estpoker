#!/usr/bin/env sh
set -e

# Prüfen, ob Node verfügbar ist
if ! command -v node >/dev/null 2>&1; then
  echo "[pre-push] Node nicht gefunden – bitte Node installieren/ins PATH legen." >&2
  exit 1
fi

echo "[pre-push] Checking templates for native title=\"...\" attributes…"
node scripts/check-no-title.js
