{
  "name": "ep-noxvobiscum-guardrails",
  "private": true,
  "scripts": {
    "check:no-title": "node scripts/check-no-title.js",
    "check:menu-events": "node scripts/check-menu-toggle-events.js",
    "check": "npm run check:no-title && npm run check:menu-events",
    "ci:verify": "npm run check"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
